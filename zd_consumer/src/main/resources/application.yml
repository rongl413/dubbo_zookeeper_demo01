server:
  port: 7070

## Dubbo 服务提供者配置
#应用名
spring:
  dubbo:
    application:
      name: consumer
    #zookeeper的注册地址
    registry:
      address: zookeeper://127.0.0.1:2181
    #将消费者的控制层路径映射到dubbo中并由它管理
    scan: com.java.consumer.controller